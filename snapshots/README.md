# Snapshots

Put user-uploaded GKD snapshot zip files here when they need to be shared through Git.

This directory is intentionally not ignored by Git, and GitHub Actions is configured to ignore `snapshots/**` changes for check/deploy workflows.
